# Withdrawn H28-C pilot output

The r2 output is invalid and must not be interpreted. Its support and query
functions received canonical rather than observed-coordinate inputs. The oracle
adapter therefore showed nonzero error. This was detected by the oracle control
before any conclusion was drawn. The corrected run is r3; no r2 number is used.
